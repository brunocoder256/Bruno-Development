const $=(s,p=document)=>p.querySelector(s);const $$=(s,p=document)=>[...p.querySelectorAll(s)];
const menu=$('#menuToggle'),nav=$('#mainNav');
menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open);menu.setAttribute('aria-label',open?'Close menu':'Open menu');});
$$('.nav a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));
const progress=$('#scrollProgress');window.addEventListener('scroll',()=>{const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${h>0?(scrollY/h)*100:0}%`},{passive:true});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(el=>observer.observe(el));
const counters=$$('[data-count]');const countObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=+el.dataset.count;let start=0;const duration=1000;const tick=t=>{start=Math.min(target,Math.round((t-(el._start||t))/duration*target));el.textContent=start;if(start<target)requestAnimationFrame(tick)};el._start=performance.now();requestAnimationFrame(tick);countObserver.unobserve(el)}),{threshold:.8});counters.forEach(c=>countObserver.observe(c));
const heroImages=[$('#heroImageA'),$('#heroImageB')];let heroIndex=0;setInterval(()=>{heroIndex=(heroIndex+1)%heroImages.length;heroImages.forEach((img,i)=>img?.classList.toggle('active',i===heroIndex));const tag=$('.image-tag');if(tag)tag.innerHTML=`<span>0${heroIndex+1}</span> ${heroIndex?'FOCUS MODE':'BUILDER MODE'}`},5000);
$$('.filter').forEach(btn=>btn.addEventListener('click',()=>{$$('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const filter=btn.dataset.filter;$$('.project-card').forEach(card=>card.classList.toggle('is-hidden',filter!=='all'&&card.dataset.category!==filter))}));
const form=$('#projectForm');form?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=(data.get('name')||'').trim();const type=data.get('type');const msg=(data.get('message')||'').trim();const text=`Hello Bruno, I'm ${name}. I want to discuss a ${type}.%0A%0AProject brief:%0A${encodeURIComponent(msg)}`;window.open(`https://wa.me/256768082948?text=${text}`,'_blank','noopener');});
const glow=$('#cursorGlow');window.addEventListener('pointermove',e=>{if(glow){glow.style.left=`${e.clientX}px`;glow.style.top=`${e.clientY}px`}});
$('#year').textContent=new Date().getFullYear();
